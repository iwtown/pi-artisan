I've set up parallel research with the `researcher` subagent focusing on best practices for managing version upgrades of capability packages, and the `oracle` subagent providing direction on whether capability package boundary detection and auto-enabling should be solved within pi-artisan. Let me now describe the research plan and expected outcomes.
The parallel research is set up as follows:

1. **researcher**: This subagent will focus on identifying best practices for managing version upgrades of capability packages, including skills, extensions, prompts, themes, and packages. It will research how other AI agent ecosystems handle version management, adaptation, and the challenges and opportunities they face in this area.

2. **oracle**: This subagent will provide insights on whether the detection of capability package boundaries and the auto-enabling of these packages should be handled within pi-artisan or if it's better to explore alternative solutions or modifications to the existing system. It will analyze the feasibility and potential benefits of integrating this functionality directly within pi-artisan or suggesting alternative approaches if necessary.

The expected outcomes of the research will be a comprehensive report detailing the findings from the `researcher` and `oracle` subagents, providing actionable insights for the user. The report will cover:
- The current best practices for managing version upgrades
- The current approaches for capability package boundary detection and auto-enabling
- The feasibility and potential benefits of integrating this functionality within pi-artisan
- Alternative solutions or modifications to the existing system
- Recommendations for the user's specific needs and goals
- The `researcher` subagent will conduct the research by querying the `high-context decision-consistency oracle` subagent, which will provide context-aware recommendations based on the specific question the user has about the project.
- The `oracle` subagent will offer insights on whether the detection of capability package boundaries and the auto-enabling of these packages should be handled within pi-artisan or if it's better to explore alternative solutions or modifications to the existing system.
- The research will be conducted in parallel and the results will be synthesized into a comprehensive report for the user.

The user can then use this report to make informed decisions about how to manage version upgrades and capability package boundaries and auto-enabling.